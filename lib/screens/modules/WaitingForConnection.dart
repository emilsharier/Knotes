import 'package:flutter/material.dart';

class WaitingForConnection extends StatelessWidget {
  String str = "Waiting for connection. Please check you internet connection.";

  WaitingForConnection({this.str});
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
